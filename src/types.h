#ifndef LEMONS_TYPES_H
#define LEMONS_TYPES_H

#include <string>
#include <sstream>

namespace lem
{
    //using Bytes = std::basic_string<uint8_t>;
    //using ByteStream = std::basic_stringstream<uint8_t>;

    using Bytes = std::string;
    using ByteStream = std::stringstream;
}

#endif //LEMONS_TYPES_H
